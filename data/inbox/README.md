# Inbox

Place unorganized user-provided information here before normalization.

## Rules

- Preserve original files and content.
- Do not overwrite originals during normalization.
- After review, copy structured facts into `data/master/` or flag for human review.
- Inbox content is gitignored except this README — do not commit raw private material.

See `AGENTS.md` for master-data protection rules.
